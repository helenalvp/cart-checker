import { Fragment, Component } from "react";

class AppIndex extends Component {
  render() {
    return (
      <Fragment>
        <div className="text-muted">This is where the app goes</div>
      </Fragment>
    );
  }
}

export default AppIndex;
